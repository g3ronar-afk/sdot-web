"scripts": {
  "build": "echo 'No build step required'",
  "prebuild": "npm rebuild sqlite3 --build-from-source",
  "postbuild": "cp -r node_modules/sqlite3 functions/node_modules/ 2>/dev/null || :",
  "netlify-build": "npm install --production && npm run prebuild"
}